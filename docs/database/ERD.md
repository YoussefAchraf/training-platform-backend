# Entity-relationship diagrams

> Generated by `npm run docs:db` from a migrated database. Do not edit by hand.
> Crow's-foot notation: `||` exactly one, `o|` zero or one, `o{` zero or many. A solid line is an identifying
> relationship (the foreign key is part of the child's primary key); a dotted line is a plain reference.
> Only key columns are drawn; see [SCHEMA-REFERENCE.md](SCHEMA-REFERENCE.md) for every column.

## Whole schema

```mermaid
erDiagram
  audit_log {
    int id PK
    int actor_id FK
  }
  calendar {
    int id PK
    int session_id FK
  }
  clients {
    int id PK
    int created_by FK
  }
  conversation_participants {
    int conversation_id PK,FK
    int user_id PK,FK
    int last_delivered_message_id FK
    int last_read_message_id FK
  }
  conversations {
    int id PK
    int created_by FK
  }
  feature_announcement_ratings {
    int id PK
    int announcement_id FK
    int user_id FK
  }
  feature_announcement_target_roles {
    int announcement_id PK,FK
    int role_id PK,FK
  }
  feature_announcements {
    int id PK
    int created_by FK
  }
  feedback_reports {
    int id PK
    int submitted_by FK
  }
  instructor_skills {
    int instructor_id PK,FK
    int training_id PK,FK
  }
  instructors {
    int id PK
    int user_id FK,UK
  }
  message_deletions {
    int message_id PK,FK
    int user_id PK,FK
  }
  messages {
    int id PK
    int conversation_id FK
    int reply_to_message_id FK
    int sender_id FK
  }
  providers {
    int id PK
    int created_by FK
  }
  push_subscriptions {
    int id PK
    int user_id FK
  }
  reports {
    int id PK
    int session_id FK,UK
  }
  roles {
    int id PK
    varchar(50) name UK
  }
  session_attendees {
    int id PK
    int session_id FK
  }
  session_notes {
    int id PK
    int instructor_id FK
    int session_id FK
  }
  surveys {
    int id PK
    int attendee_id FK
    int instructor_id FK
    int session_id FK
  }
  training_sessions {
    int id PK
    int client_id FK
    int created_by FK
    int instructor_id FK
    int training_id FK
  }
  trainings {
    int id PK
    int created_by FK
    int provider_id FK
  }
  users {
    int id PK
    int approved_by FK
    varchar(150) email UK
    int role_id FK
  }
  clients ||..o{ training_sessions : "client_id"
  conversations ||--o{ conversation_participants : "conversation_id"
  conversations ||..o{ messages : "conversation_id"
  feature_announcements ||--o{ feature_announcement_target_roles : "announcement_id"
  feature_announcements ||..o{ feature_announcement_ratings : "announcement_id"
  instructors o|..o{ surveys : "instructor_id"
  instructors o|..o{ training_sessions : "instructor_id"
  instructors ||--o{ instructor_skills : "instructor_id"
  instructors ||..o{ session_notes : "instructor_id"
  messages o|..o{ conversation_participants : "last_delivered_message_id"
  messages o|..o{ conversation_participants : "last_read_message_id"
  messages o|..o{ messages : "reply_to_message_id"
  messages ||--o{ message_deletions : "message_id"
  providers ||..o{ trainings : "provider_id"
  roles ||--o{ feature_announcement_target_roles : "role_id"
  roles ||..o{ users : "role_id"
  session_attendees o|..o{ surveys : "attendee_id"
  training_sessions ||..o{ calendar : "session_id"
  training_sessions ||..o{ session_attendees : "session_id"
  training_sessions ||..o{ session_notes : "session_id"
  training_sessions ||..o{ surveys : "session_id"
  training_sessions ||..o| reports : "session_id"
  trainings ||--o{ instructor_skills : "training_id"
  trainings ||..o{ training_sessions : "training_id"
  users o|..o{ audit_log : "actor_id"
  users o|..o{ clients : "created_by"
  users o|..o{ conversations : "created_by"
  users o|..o{ feature_announcements : "created_by"
  users o|..o{ feedback_reports : "submitted_by"
  users o|..o{ messages : "sender_id"
  users o|..o{ providers : "created_by"
  users o|..o{ training_sessions : "created_by"
  users o|..o{ trainings : "created_by"
  users o|..o{ users : "approved_by"
  users ||--o{ conversation_participants : "user_id"
  users ||--o{ message_deletions : "user_id"
  users ||..o{ feature_announcement_ratings : "user_id"
  users ||..o{ push_subscriptions : "user_id"
  users ||..o| instructors : "user_id"
```

## Identity, notifications and audit

```mermaid
erDiagram
  audit_log {
    int id PK
    int actor_id FK
  }
  clients {
    int id PK
  }
  conversation_participants {
    int conversation_id PK,FK
    int user_id PK,FK
  }
  conversations {
    int id PK
  }
  feature_announcement_ratings {
    int id PK
    int announcement_id FK
    int user_id FK
  }
  feature_announcement_target_roles {
    int announcement_id PK,FK
    int role_id PK,FK
  }
  feature_announcements {
    int id PK
    int created_by FK
  }
  feedback_reports {
    int id PK
    int submitted_by FK
  }
  instructors {
    int id PK
  }
  message_deletions {
    int message_id PK,FK
    int user_id PK,FK
  }
  messages {
    int id PK
  }
  providers {
    int id PK
  }
  push_subscriptions {
    int id PK
    int user_id FK
  }
  roles {
    int id PK
    varchar(50) name UK
  }
  training_sessions {
    int id PK
  }
  trainings {
    int id PK
  }
  users {
    int id PK
    int approved_by FK
    varchar(150) email UK
    int role_id FK
  }
  feature_announcements ||--o{ feature_announcement_target_roles : "announcement_id"
  feature_announcements ||..o{ feature_announcement_ratings : "announcement_id"
  roles ||--o{ feature_announcement_target_roles : "role_id"
  roles ||..o{ users : "role_id"
  users o|..o{ audit_log : "actor_id"
  users o|..o{ clients : "created_by"
  users o|..o{ conversations : "created_by"
  users o|..o{ feature_announcements : "created_by"
  users o|..o{ feedback_reports : "submitted_by"
  users o|..o{ messages : "sender_id"
  users o|..o{ providers : "created_by"
  users o|..o{ training_sessions : "created_by"
  users o|..o{ trainings : "created_by"
  users o|..o{ users : "approved_by"
  users ||--o{ conversation_participants : "user_id"
  users ||--o{ message_deletions : "user_id"
  users ||..o{ feature_announcement_ratings : "user_id"
  users ||..o{ push_subscriptions : "user_id"
  users ||..o| instructors : "user_id"
```

## Catalog: providers, trainings, clients, instructors

```mermaid
erDiagram
  clients {
    int id PK
    int created_by FK
  }
  instructor_skills {
    int instructor_id PK,FK
    int training_id PK,FK
  }
  instructors {
    int id PK
    int user_id FK,UK
  }
  providers {
    int id PK
    int created_by FK
  }
  session_notes {
    int id PK
  }
  surveys {
    int id PK
  }
  training_sessions {
    int id PK
  }
  trainings {
    int id PK
    int created_by FK
    int provider_id FK
  }
  users {
    int id PK
  }
  clients ||..o{ training_sessions : "client_id"
  instructors o|..o{ surveys : "instructor_id"
  instructors o|..o{ training_sessions : "instructor_id"
  instructors ||--o{ instructor_skills : "instructor_id"
  instructors ||..o{ session_notes : "instructor_id"
  providers ||..o{ trainings : "provider_id"
  trainings ||--o{ instructor_skills : "training_id"
  trainings ||..o{ training_sessions : "training_id"
  users o|..o{ clients : "created_by"
  users o|..o{ providers : "created_by"
  users o|..o{ trainings : "created_by"
  users ||..o| instructors : "user_id"
```

## Delivery: sessions, attendees, surveys, reports

```mermaid
erDiagram
  calendar {
    int id PK
    int session_id FK
  }
  clients {
    int id PK
  }
  instructors {
    int id PK
  }
  reports {
    int id PK
    int session_id FK,UK
  }
  session_attendees {
    int id PK
    int session_id FK
  }
  session_notes {
    int id PK
    int instructor_id FK
    int session_id FK
  }
  surveys {
    int id PK
    int attendee_id FK
    int instructor_id FK
    int session_id FK
  }
  training_sessions {
    int id PK
    int client_id FK
    int created_by FK
    int instructor_id FK
    int training_id FK
  }
  trainings {
    int id PK
  }
  users {
    int id PK
  }
  clients ||..o{ training_sessions : "client_id"
  instructors o|..o{ surveys : "instructor_id"
  instructors o|..o{ training_sessions : "instructor_id"
  instructors ||..o{ session_notes : "instructor_id"
  session_attendees o|..o{ surveys : "attendee_id"
  training_sessions ||..o{ calendar : "session_id"
  training_sessions ||..o{ session_attendees : "session_id"
  training_sessions ||..o{ session_notes : "session_id"
  training_sessions ||..o{ surveys : "session_id"
  training_sessions ||..o| reports : "session_id"
  trainings ||..o{ training_sessions : "training_id"
  users o|..o{ training_sessions : "created_by"
```

## Messaging

```mermaid
erDiagram
  conversation_participants {
    int conversation_id PK,FK
    int user_id PK,FK
    int last_delivered_message_id FK
    int last_read_message_id FK
  }
  conversations {
    int id PK
    int created_by FK
  }
  message_deletions {
    int message_id PK,FK
    int user_id PK,FK
  }
  messages {
    int id PK
    int conversation_id FK
    int reply_to_message_id FK
    int sender_id FK
  }
  users {
    int id PK
  }
  conversations ||--o{ conversation_participants : "conversation_id"
  conversations ||..o{ messages : "conversation_id"
  messages o|..o{ conversation_participants : "last_delivered_message_id"
  messages o|..o{ conversation_participants : "last_read_message_id"
  messages o|..o{ messages : "reply_to_message_id"
  messages ||--o{ message_deletions : "message_id"
  users o|..o{ conversations : "created_by"
  users o|..o{ messages : "sender_id"
  users ||--o{ conversation_participants : "user_id"
  users ||--o{ message_deletions : "user_id"
```
